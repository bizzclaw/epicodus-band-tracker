@{
  Layout = "_Layout";
}

<div class="jumbotron">
	<h1>@Model.GetName()</h1>
</div>

<h3>Id: @Model.GetId()</h3>

<p>This band is entered into the Database</p>
<a href="/bands"><button type="submit" class="btn btn-default btn-wide">Back to Bands</button></a>
